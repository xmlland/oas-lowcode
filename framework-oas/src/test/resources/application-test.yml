spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  redis:
    host: localhost
    port: 6379

# 最小化配置，其他配置由 Mock 覆盖
server:
  port: 0

logging:
  level:
    root: WARN
    com.jeestudio.bpm: DEBUG

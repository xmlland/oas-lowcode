import {FormControlProps} from "@/dynamic/DynamicFormItemConfig";

class IconSelectProps extends FormControlProps {

    /**
     * 是否显示输入框
     * @type {boolean}
     */
    showInput = false

    constructor() {
        super();
    }
}

export default IconSelectProps
